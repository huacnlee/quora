<%= content_for :scripts do %>
  <script type="text/javascript">
    var current_page = 2;
    function loadAsks(){
      App.loading();
      $.get("<%= request.path %>.js?page="+current_page,function(){
          App.loading(false);
      });
      return false;
    }
  </script>
<% end %>
<div class="uname">
	<div class="avatar"><%= image_tag(user.avatar.normal.url) %></div>
	<div class="detail">
		<h1 id="user_name"><%= user.name %><span>，</span><span id="user_tagline"><%= user.tagline %></span>
    <% if owner?user %>
      <%= in_place_edit_tag(user,:tagline,:rich => false, :text_id => "user_tagline") %> 
    <% end %></h1>
		<% if !user.website.blank? %>
		  <div class="website">
		    <a href="<%= user.website %>" rel="nofollow" target="_blank" id="user_website"><%= user.website %></a>
		    <% if owner?user %> <%= in_place_edit_tag(user,:website, :text_id => "user_website") %> <% end %>
		  </div>
		<% end %>
		<div class="bio">
			<span id="user_bio">
	      <% if @user.bio.blank? and (not owner?@user) %>
	        <%= @user.name %><%=t(:have_not_had_time_to_write_his_personal_introduction)%>.
	      <% else %>
	        <%= md_body @user.bio %>
	      <% end %>
	    </span>
	    <% if owner?@user %> <%= in_place_edit_tag(@user,:bio,:type => :textarea, :text_id => "user_bio", :label => "#{t(:modify_personal_experience)}") %> <% end %>
		</div>
	</div>
</div>

<div class="tabs">
  <a<%= ' class=actived' if params[:action] == "show" %> href="<%= user_path(user.slug) %>"><%=t(:home)%></a>
	<a<%= ' class=actived' if params[:action] == "asked_to" %> href="<%= asked_to_user_path(user.slug) %>"><%=t(:q)%><%= current_user == user ? "#{t(:i)}" : (user.girl ? "#{t(:she)}" : "#{t(:he)}") %>(<%= Ask.normal.asked_to(user.id).count %>)</a>
  <a<%= ' class=actived' if params[:action] == "asked" %> href="<%= asked_user_path(user.slug) %>"><%=t(:q)%>过(<%= user.asks_count %>)</a>
  <a<%= ' class=actived' if params[:action] == "answered" %> href="<%= answered_user_path(user.slug) %>"><%=t(:answered)%>(<%= user.answered_ask_ids.count %>)</a>
	<!-- <a<%= ' class=actived' if params[:action] == "following" %> href="<%= following_user_path(user.slug) %>">关注(<%= user.following_ids.count %>)</a>
		<a<%= ' class=actived' if params[:action] == "followers" %> href="<%= followers_user_path(user.slug) %>">被关注(<%= user.follower_ids.count %>)</a> -->
	<a<%= ' class=actived' if params[:action] == "following_topics" %> href="<%= following_topics_user_path(user.slug) %>"><%=t(:a_topic_of_concern)%>(<%= user.followed_topic_ids.count %>)</a>
</div>

<% if not owner? user %>
<div id="ask_to_user" class="simple_form">
  <h3><%=t(:true)%><%= user.name %>提<%=t(:q)%>:</h3>
  <form action="/asks" method="post">
    <div class="row">
      <%= auth_token %>
      <input type="hidden" name="ask[to_user_id]" value="<%= user.id %>" />
      <textarea type="text" class="text" onfocus="$(this).parent().next().show();" name="ask[title]"></textarea>
    </div>
    <div style="display:none">
      <div><a href="#" onclick="$(this).parent().next().show(); $(this).hide(); return false;">添加<%=t(:q)%>题描述</a></div>
      <div class="row" style="display:none">
        <label><%=t(:q)%>题描述(<%=t(:optional)%>):</label>
        <textarea type="text" class="text" name="ask[body]"></textarea>
      </div>
      <div class="actions">
        <button type="submit">提交<%=t(:q)%>题</button>
      </div>
    </div>
  </form>
</div>
<br>
<% end %>
