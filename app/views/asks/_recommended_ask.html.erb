<div class="ask" ask-id="<%= item.id %>">
  <div class="title"><a href="/asks/<%= item.id %>"><%= item.title %></a></div>
    <div class="info">
      <%=t(:the_problem_is_added_to_the)%> <%= raw item.topics.map {|t| "<a href=\"/topics/#{t}\">#{t}</a>"}.join("、") %> <%=t(:this)%><%= item.topics.size %><%=t(:topics)%>
    </div> 
</div>
